---
// Base layout: masthead, nav, footer, and site-wide structured data.
export interface Props {
  title?: string;
  description?: string;
  path?: string;
}
import '../styles/global.css';
import Newsletter from '../components/Newsletter.astro';
const {
  title = 'ArtempireMJ',
  description = 'The world, told through its artists. ArtempireMJ reports the world through the makers who carry its stories, and keeps the Global Art Calendar of open calls, fairs, residencies and grants worldwide, free for artists.',
  path = '/',
} = Astro.props;
const site = 'https://artempiremj.com';
const canonical = site + path;
const fullTitle = title === 'ArtempireMJ' ? 'ArtempireMJ · the world, told through artists' : `${title} · ArtempireMJ`;

// Organization + WebSite structured data, emitted site-wide.
const org = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': site + '/#org',
      name: 'ArtempireMJ',
      alternateName: 'ArtempireMJ · the world, told through artists',
      url: site,
      description: 'Reports the world through the artists who live its stories. The events that move the world, war, currency, migration, culture, told through the maker who carries it. Keeps the Global Art Calendar of open calls, residencies, grants, fairs and biennials worldwide, free for artists in any medium.',
      knowsAbout: ['world news through art', 'world events through artists', 'open calls', 'artist residencies', 'art grants', 'art fairs', 'biennials', 'African and Global South art', 'art and finance', 'art and migration', 'global art'],
      areaServed: 'Worldwide',
    },
    {
      '@type': 'WebSite',
      '@id': site + '/#website',
      url: site,
      name: 'ArtempireMJ',
      publisher: { '@id': site + '/#org' },
      inLanguage: 'en',
    },
  ],
};
const nav = [
  { href: '/', label: 'Calendar' },
  { href: '/world', label: 'The World' },
  { href: '/spotlight', label: 'Spotlight' },
  { href: '/discover', label: 'DISCOVER' },
  { href: '/festivals', label: 'Festivals' },
  { href: '/itineraries', label: 'Art Itineraries' },
  { href: '/artists', label: 'Artists' },
  { href: '/submit', label: 'Submit' },
];
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700&display=swap" rel="stylesheet" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <script is:inline>
      // Travelpayouts / Trip.com affiliate tracking (marker 544313). Async, non-blocking.
      (function () { var s = document.createElement('script'); s.async = 1; s.src = 'https://emrldtp.com/NTQ0MzEz.js?t=544313'; document.head.appendChild(s); })();
    </script>
    <script>
      // Kill any stale service worker / cache from a previous version of the site.
      if ('serviceWorker' in navigator) navigator.serviceWorker.getRegistrations().then((rs) => { for (const r of rs) r.unregister(); });
      if (typeof caches !== 'undefined' && caches.keys) caches.keys().then((ks) => { for (const k of ks) caches.delete(k); });
    </script>
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="ArtempireMJ" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={site + '/images/khethiwe_fnb_artjoburg_aerial.jpg'} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={site + '/images/khethiwe_fnb_artjoburg_aerial.jpg'} />
    <set:html set:html={`<script type="application/ld+json">${JSON.stringify(org)}</script>`} />
  </head>
  <body>
    <header class="masthead">
      <div class="wrap masthead-row">
        <a class="mark" href="/">
          <img class="mark-logo" src="/logo-mark.png" alt="" width="36" height="36" />
          <span>ArtempireMJ</span>
        </a>
        <nav class="nav" aria-label="Primary">
          {nav.map((n) => <a href={n.href} aria-current={n.href === path ? 'page' : undefined}>{n.label}</a>)}
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="footer">
      <div class="wrap footer-grid">
        <div class="prose">
          <p class="mark-sm">ArtempireMJ</p>
          <p class="muted">The world, told through artists.</p>
        </div>
        <div>
          <p class="eyebrow">The newsletter</p>
          <p class="muted">The week in world art, told through the makers who carry it. One email.</p>
          <Newsletter variant="inline" />
        </div>
        <div>
          <p class="eyebrow">For partners</p>
          <p class="muted"><a href="/sponsor">Reach the world that follows art, through The World, DISCOVER, and Itineraries.</a></p>
          <p class="muted"><a href="/list">List an opportunity or event</a></p>
          <p class="muted"><a href="/about">About</a></p>
          <p class="muted"><a href="/faq">Questions</a></p>
          <p class="muted"><a href="/correspondents">Report from your city</a></p>
          <p class="muted"><a href="/studio">The Studio</a></p>
        </div>
      </div>
    </footer>

    <style>
      .masthead { border-bottom: 1px solid var(--line); background: var(--cream); }
      .masthead-row { display: flex; align-items: baseline; justify-content: space-between; gap: 1rem; padding-block: 1.1rem; flex-wrap: wrap; }
      .mark { font-family: var(--display); font-weight: 600; font-size: 1.5rem; letter-spacing: -0.02em; text-decoration: none; display: inline-flex; align-items: center; gap: 0.55rem; color: var(--ink); }
      .mark-logo { width: 36px; height: 36px; flex: none; border-radius: 50%; }
      .masthead { border-bottom: 1px solid var(--line); background: var(--cream); border-top: 3px solid var(--teal); }
      .nav { display: flex; gap: 1.15rem; font-family: var(--sans); font-size: 0.9rem; flex-wrap: wrap; }
      .nav a { text-decoration: none; color: var(--ink-soft); }
      .nav a[aria-current="page"] { color: var(--orange); font-weight: 600; }
      .footer { border-top: 1px solid var(--line); background: var(--cream); margin-top: 4rem; }
      .footer-grid { display: grid; grid-template-columns: 1.4fr 1fr 1fr; gap: 2rem; padding-block: 2.5rem; }
      @media (max-width: 720px) { .footer-grid { grid-template-columns: 1fr; } }
      .mark-sm { font-family: var(--serif); font-weight: 600; font-size: 1.15rem; margin: 0 0 0.2rem; }
      .sub { display: flex; gap: 0.5rem; margin-top: 0.5rem; }
      .sub input { flex: 1; padding: 0.6em 0.8em; border: 1px solid var(--line); border-radius: 2px; font-family: var(--sans); }
    </style>
    <script is:inline defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token": "9b0d93f4cb934625bba3c44ad313ead5"}'></script>
  </body>
</html>
